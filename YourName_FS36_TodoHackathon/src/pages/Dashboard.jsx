import { useMemo, useState } from 'react'
import { useTodos } from '../hooks/useTodos'
import TaskList from '../components/TaskList'
import TaskForm from '../components/TaskForm'
export default function Dashboard(){const{todos,filteredTodos,search,setSearch,sort,setSort,apiLoading}=useTodos();const[filter,setFilter]=useState('all');const[adding,setAdding]=useState(false);const visible=useMemo(()=>filteredTodos.filter(t=>filter==='all'||(filter==='active'&&!t.completed)||(filter==='completed'&&t.completed)),[filteredTodos,filter]);return <section><div className="section-head"><div><p className="kicker">YOUR WORKSPACE</p><h2>All tasks</h2></div><div className="counts"><span>{todos.length} total</span><span>{todos.filter(t=>!t.completed).length} active</span></div></div><div className="toolbar"><div className="search">⌕<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search tasks..."/></div><select value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest</option><option value="title">Title</option><option value="priority">Priority</option></select></div><div className="filters">{['all','active','completed'].map(x=><button className={filter===x?'selected':''} key={x} onClick={()=>setFilter(x)}>{x[0].toUpperCase()+x.slice(1)}</button>)}</div>{apiLoading&&<p className="kicker">SYNCING STARTER TASKS…</p>}<TaskList tasks={visible}/><button className="add-inline" onClick={()=>setAdding(true)}>＋ Add another task</button>{adding&&<TaskForm onClose={()=>setAdding(false)}/>}</section>}
