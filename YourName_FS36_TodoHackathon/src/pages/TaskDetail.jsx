import { Link, useNavigate, useParams } from 'react-router-dom'
import { useTodos } from '../hooks/useTodos'
export default function TaskDetail(){const{id}=useParams(),nav=useNavigate(),{todos,toggleTodo,deleteTodo}=useTodos(),task=todos.find(t=>t.id===id);if(!task)return <div className="empty"><strong>Task not found</strong><button className="primary" onClick={()=>nav('/')}>Back to tasks</button></div>;return <section className="detail"><Link to="/" className="back">← Back to all tasks</Link><p className="kicker">DYNAMIC ROUTE / TASK DETAIL</p><h2>{task.title}</h2><p className="detail-copy">{task.description||'No description added.'}</p><div className="detail-grid"><div><span>STATUS</span><strong>{task.completed?'Completed':'Active'}</strong></div><div><span>PRIORITY</span><strong>{task.priority}</strong></div><div><span>DUE DATE</span><strong>{task.dueDate||'No due date'}</strong></div></div><div className="detail-actions"><button className="primary" onClick={()=>toggleTodo(task.id)}>{task.completed?'Mark active':'Mark complete'}</button><button className="danger-outline" onClick={()=>{deleteTodo(task.id);nav('/')}}>Delete</button></div></section>}
