import { useTodos } from '../hooks/useTodos'
export default function Stats(){const{todos}=useTodos();const total=todos.length,done=todos.filter(t=>t.completed).length,active=total-done,high=todos.filter(t=>t.priority==='High'&&!t.completed).length,pct=total?Math.round(done/total*100):0;return <section><div className="section-head"><div><p className="kicker">ROUTE / ANALYTICS</p><h2>Progress snapshot</h2></div></div><div className="stats-grid"><div><span>Total</span><strong>{total}</strong></div><div><span>Active</span><strong>{active}</strong></div><div><span>Completed</span><strong>{done}</strong></div><div><span>High priority</span><strong>{high}</strong></div></div><div className="progress"><div className="progress-top"><b>Completion rate</b><strong>{pct}%</strong></div><div className="bar"><i style={{width:`${pct}%`}}/></div></div></section>}
