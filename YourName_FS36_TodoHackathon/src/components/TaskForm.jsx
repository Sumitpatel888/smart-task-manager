import { useRef, useState } from 'react'
import { useTodos } from '../hooks/useTodos'
export default function TaskForm({onClose,task}){const{addTodo,updateTodo}=useTodos();const inputRef=useRef(null);const[form,setForm]=useState(task||{title:'',description:'',priority:'Medium',dueDate:''});const submit=e=>{e.preventDefault();if(!form.title.trim())return;task?updateTodo(task.id,form):addTodo(form);onClose()};return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><form className="modal" onSubmit={submit}><div className="modal-head"><div><p className="kicker">TASK EDITOR</p><h2>{task?'Edit task':'Create a task'}</h2></div><button type="button" onClick={onClose}>×</button></div><label>Title<input ref={inputRef} autoFocus value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="What needs to be done?"/></label><label>Description<textarea rows="4" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Add useful context..."/></label><div className="form-grid"><label>Priority<select value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}><option>Low</option><option>Medium</option><option>High</option></select></label><label>Due date<input type="date" value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})}/></label></div><div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>Cancel</button><button className="primary">{task?'Save changes':'Add task'}</button></div></form></div>}
